# EIMSレビュー時の判断メモ

作成日: 2026-06-15

## 目的

この文書は、すでに修正済みの不整合を列挙するものではありません。

別のレビューで「ここも直すべきではないか」と再指摘されそうな点のうち、教材の簡易仕様として、または追加機能との切り分けとして、あえて修正対象にしない判断をまとめたものです。

## あえて修正しない判断

### 性別は `@NotNull` のみとする

`gender` は画面上ではラジオボタンで入力します。そのため、通常の画面操作では定義済みの値以外が送信される想定はありません。

Basicの簡易仕様では、性別が未選択でないことだけを確認できれば十分です。`@Min` / `@Max` などで `1` と `2` 以外を弾く厳密なチェックは、今回は追加しません。

再指摘時の回答:

> 性別はラジオボタン入力なので、簡易仕様では `@NotNull` のみで扱います。画面操作で発生しない値の厳密チェックは今回の範囲外です。

### パスワードに半角英数字制約は設けない

パスワードは必須、文字数などの基本的な条件を確認できればよく、半角英数字であることまではBasicの簡易仕様では求めません。

半角英数字制約を入れると、教材として扱うバリデーション観点が増えます。今回は入力必須や文字数など、主要なBean Validationの理解を優先します。

再指摘時の回答:

> パスワードの半角英数字制約は簡易仕様では扱いません。教材としては必須・文字数チェックを中心にしています。

### 確認画面のパスワードマスクは文字数一致を求めない

確認画面では、パスワードを `********` のように固定文字列で非表示にできれば十分です。

実際のパスワード文字数と `*` の数を一致させる必要はありません。むしろ文字数が分かる表示より、固定マスクの方が実装も説明も簡単です。

再指摘時の回答:

> パスワードは固定マスクで非表示にする方針です。`*` の数を実際の文字数に合わせる仕様ではありません。

### 社員一覧の表示順は保証しない

社員一覧は、社員番号昇順を必須仕様にしません。

今回のBasicでは、一覧表示そのものを確認できれば十分であり、表示順にこだわりはありません。並び順を保証したい場合は `Sort` 指定などを追加できますが、今回は仕様範囲外です。

再指摘時の回答:

> 社員一覧の表示順は仕様上の制約にしていません。昇順保証は今回のBasic仕様では求めません。

### 削除対象が `null` の場合の細かい分岐は扱わない

存在しない社員番号を指定した場合に、削除できないことを画面上で示す扱いは入れています。

ただし、削除処理に渡る値そのものが `null` の場合など、細かい異常系の分岐は簡易仕様では深追いしません。教材としては、通常の画面遷移と主要な異常系を理解できることを優先します。

再指摘時の回答:

> 存在しない社員の扱いは入れていますが、`null` 入力時の詳細分岐までは簡易仕様では扱いません。

### `role` と `delete_flg` はBasic仕様書に書かない

`role` と `delete_flg` は追加機能のために用意している項目です。

Basicの簡易仕様には直接関係しないため、仕様書には記載しません。Basic仕様書に書くと、学習者が今回実装する範囲と追加機能の範囲を混同しやすくなります。

再指摘時の回答:

> `role` と `delete_flg` は追加機能用の項目なので、Basic仕様書には記載しません。

### `employee.dept_no` のDB制約は簡易仕様でエラーになりにくい形を優先する

画面側では部署を必須入力として扱いますが、DB構築では、簡易仕様の組み合わせによって不要な `NOT NULL` エラーが起きないことを優先します。

学習者が一部の簡易仕様だけを選んで実装した場合でも、DB制約が原因で本筋と関係ないエラーに詰まらないようにするためです。

再指摘時の回答:

> 部署は画面入力上は必須ですが、DB構築では簡易仕様の組み合わせで不要な制約エラーが出ないことを優先しています。

## レビュー者へ伝える要約

今回の方針は、「すべてを厳密に作り込む」ことではなく、Basic教材として自然に学習できる範囲へ絞ることです。

特に、性別の厳密な値チェック、パスワードの半角英数字制約、パスワードマスクの文字数一致、社員一覧の昇順保証、`null` 入力時の細かい分岐、`role` / `delete_flg` のBasic仕様書への記載は、あえて採用しない判断です。

# EIMSレビュー指摘への対応方針メモ

作成日: 2026-06-15

## 目的

この文書は、`EIMS_Comprehensive_Exercise_Guide.md` と `EIMS_SpringBoot_Basic` のレビューで出た指摘について、どの指摘を仕様書に合わせるのか、どの指摘をソースコードに合わせるのか、または簡易仕様としてあえて扱わないのかを整理したものです。

今後、別のレビューで同じ指摘が出た場合は、本資料を「判断済みの方針」として参照してください。

## 基本方針

- 仕様書、ソースコード、DB構築SQLのいずれを正とするかは、学習用の簡易仕様としての扱いやすさを優先して決定する。
- 追加機能用の項目は、Basicの簡易仕様に直接関係しない場合、仕様書へ無理に追記しない。
- 画面操作上発生しにくい不正値は、簡易仕様では過度に扱わない。
- DBの `NOT NULL` 制約により、簡易仕様の実装時に不要なエラーが出ないことを優先する。
- 既存実装の方が教材として自然で簡単な場合は、仕様書を実装に合わせる。

## 指摘別の判断

| No | 指摘内容 | 採用した方針 | 理由 | 同じ指摘が出た場合の回答 |
| --- | --- | --- | --- | --- |
| 1 | DB接続パスワードが `createUser.sql` とアプリ設定で一致していない | `createUser.sql` を正とし、ソースコードを修正 | DB構築手順で作成するユーザー情報が実行環境の前提になるため | DB構築SQLを正として、アプリ設定を合わせています |
| 2 | バリデーション仕様と `EmployeeForm` の実装が一致していない | パスワードの半角英数字仕様は削除。`deptNo` は `@NotNull` を追加。`gender` は `@NotNull` のみ | パスワードの半角英数字制約は簡易仕様として不要。性別はラジオボタンなので通常は定義外の値が入らないため、過度な制約は入れない | `gender` は画面仕様上ラジオボタン入力のため、`@NotNull` のみで十分と判断しています |
| 3 | 社員一覧が社員番号昇順と書かれているが実装で保証されていない | 昇順制約を仕様書から削除 | 一覧の表示順に教材上のこだわりはなく、昇順保証を実装に強制する必要がないため | 一覧順は仕様上の制約にしていません |
| 4 | 検索条件未入力時に検索画面へ戻す処理で部署リストが不足する可能性がある | ソースコードを修正 | 検索画面には部署プルダウンが必要であり、画面再表示時にも `departments` が必要なため | これは画面再表示に必要なデータなので実装修正済みです |
| 5 | 削除対象が存在しない場合の仕様・実装が不足している | ソースコードと仕様書を追加。ただし簡略化仕様ではnull時の細かい分岐は扱わない | 存在しない社員を削除できないことは明示する。一方で、null時の細かな分岐は教材として難しくなるため、簡易仕様では深追いしない | 存在しない社員の扱いは追加済みですが、簡易仕様ではnull分岐の詳細までは扱いません |
| 6 | 完了画面のボタン仕様が仕様書と実装で一致しない | ソースコードを仕様書に合わせて修正 | 完了後の遷移は学習者が確認しやすい重要な画面フローであり、仕様書どおりに揃えるべきため | 完了画面の遷移は仕様書を正として実装修正済みです |
| 7 | 確認画面の氏名表示形式とパスワードマスクが仕様書と実装で異なる | 仕様書をソースコードに合わせて修正。パスワードは `********` のような非表示でよく、実文字数と一致しなくてよい | パスワードの実文字数に合わせたマスクは不要で、固定マスクの方が教材として簡単。氏名・カナの別行表示も現行実装で問題ないため | パスワードは固定マスク表示でよく、文字数一致は仕様として求めません |
| 8 | 詳細画面・削除確認画面の表示項目が仕様書と実装で異なる | 詳細画面はソースコードを正として仕様書を修正。削除画面は「氏、名、かな、性別、所属部署」を表示する方針で、仕様書とソースコードを修正 | 詳細画面は現行実装の情報量が適切。削除画面は削除対象を確認するため、氏名・かな・性別・所属部署を表示する方が自然なため | 詳細画面は実装準拠、削除画面は確認に必要な項目を表示する仕様に整理済みです |
| 9 | 0件時メッセージが複数あり統一されていない | 共通メッセージ一覧を正として仕様書・実装を合わせる | メッセージは共通一覧を正とする方が、画面ごとの表記揺れを防げるため | 共通メッセージ一覧を正として統一しています |
| 10 | URLマッピング表とControllerメソッド名が一致しない | 仕様書のURLマッピング表を正として、関連箇所を修正 | URLマッピング表は設計上の基準になるため、クラス図やシーケンスなど関連記述も合わせる必要があるため | URLマッピング表を正として、関連箇所を揃えています |
| 11 | クラス図の `update(EmployeeForm)` と依存関係が実装と異なる | `update(EmployeeForm)` は正。依存関係は実装を正として全体クラス図を修正 | 変更処理として `update(EmployeeForm)` を持つ方が意図が明確。一方、`EmployeeServiceImpl` は `DepartmentRepository` に依存していないため、実装に合わせる | 変更処理は `update(EmployeeForm)`、依存関係は実装準拠です |
| 12 | テストケースが主要機能を網羅していない | 漏れているテストケースを追加 | 仕様書で定義している主要なController処理は、教材としてテスト観点にも載せるべきため | 主要な一覧、部署検索、削除、変更系のテストケースを追加済みです |
| 13 | Repositoryテストが本番DB設定に依存している | テストDBを使うようにソースコードを修正 | 本番DBをテストで使うとデータ破壊や環境依存が起きるため。`eimsdb_test` を使う方が安全 | テストはテストDBを使う方針に変更済みです |
| 14 | DB仕様、DB構築SQL、実装の定義が一致していない | `dept_no` は簡易仕様で `NOT NULL` 起因のエラーが出ないようDB構築を整理。`role` と `delete_flg` は追加機能用なのでBasic仕様書には書かない。初期データは20件。部署は仕様書に合わせる | 簡易仕様を選んで実装する際、部署未選択などでDB制約エラーになることを避けるのが最重要。追加機能用カラムをBasic仕様へ持ち込むと説明が重くなるため | `role` と `delete_flg` は追加機能用なのでBasic仕様書には記載不要です。初期データは20件で整理しています |
| 15 | ユースケース仕様の代替フロー参照に漏れがある | 仕様書を修正 | 参照漏れは読み手の混乱につながるため、簡易仕様でも正確に書くべきため | 代替フロー参照漏れは仕様書側で修正済みです |

## 特に再指摘されやすい判断

### 性別のバリデーション

`gender` はラジオボタン入力のため、通常の画面操作では `1` または `2` 以外の値は送信されません。そのため、Basicの簡易仕様では `@NotNull` のみを必須条件とし、`@Min` / `@Max` などの厳密な値チェックは入れません。

### パスワードのマスク表示

確認画面のパスワードは、実際の文字数に合わせた `*` 表示を仕様として求めません。教材としては、固定の `********` のような非表示で十分です。

### 社員一覧の表示順

社員一覧は、社員番号昇順を仕様上の必須条件にしません。表示順へのこだわりがないため、DBやJPAの返却順に依存しない仕様として整理しています。

### `role` と `delete_flg`

`role` と `delete_flg` は追加機能のために用意されている項目です。Basicの簡易仕様には直接関係しないため、仕様書には記載しません。

### `employee.dept_no` のDB制約

簡易仕様の組み合わせによっては部署未選択の状態が発生する可能性があります。その際にDBの `NOT NULL` 制約で不要なエラーにならないことを優先し、DB構築側では扱いやすい形にしています。

## レビュー者へ伝える要約

今回の修正では、すべてを一律に「仕様書が正しい」または「ソースコードが正しい」とは扱っていません。教材としての簡単さ、画面操作上の自然さ、テスト環境の安全性、追加機能との境界を踏まえて、項目ごとに正とする基準を決めています。

特に、`gender` の厳密チェック、パスワードマスクの文字数一致、社員一覧の昇順、`role` / `delete_flg` のBasic仕様書への記載は、あえて採用しない判断です。
